import { makeStyles } from '@/shared/theme/makeStyles';

export const useAnimatedHeartButtonStyles = makeStyles((_theme) => ({
  likePill: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  likeInner: {
    justifyContent: 'center',
    alignItems: 'center',
  },
}));
